node_modules/
dist/
out/
.vite/
release/

# `build/` es salida de compilación en general, pero el de apps/desktop guarda
# los recursos del instalador (el icono) y sí se versiona.
build/
!apps/desktop/build/
!apps/desktop/build/**
*.log
.DS_Store

# Notas de trabajo personales: viven solo en local, no en el repositorio.
PROGRESO.md
CLAUDE.md
.env
.env.local
coverage/
